from django.apps import AppConfig


class ChannelsBroadcastConfig(AppConfig):
    name = "channels_broadcast"
    label = "channels_broadcast"
    verbose_name = "Channels Broadcast"
    default_auto_field = "django.db.models.BigAutoField"
